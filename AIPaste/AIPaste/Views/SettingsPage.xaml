<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AIPaste.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AIPaste.Views"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <converters:BoolToObjectConverter x:Key="BoolToVisibilityConverter" TrueValue="Visible" FalseValue="Collapsed"/>
    </Page.Resources>
    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="0">
            <StackPanel>
                <TextBox Header="Key Pattern" Text="{x:Bind ViewModel.KeyPattern, Mode=TwoWay}" Margin="0,0,0,10"/>

                <ToggleSwitch Header="Auto Start" OffContent="Off" OnContent="On" IsOn="{x:Bind ViewModel.AutoStart, Mode=TwoWay}" Margin="0,0,0,10"/>

                <ComboBox Header="Model Type" x:Name="ModelTypesCombo"
                    ItemsSource="{x:Bind ViewModel.ModelTypes}"
                    DisplayMemberPath="Item1"
                    SelectedValuePath="Item2"
                    SelectedValue="{x:Bind ViewModel.ModelType, Mode=TwoWay}" 
                    Margin="0,0,0,10"
                    Loaded="ModelTypesLoaded"/>

                <StackPanel x:Name="LocalLLMSettingsPanel" Visibility="{x:Bind ViewModel.IsLocalLLMSelected, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
                    <TextBox Header="LLM Model Path" Text="{x:Bind ViewModel.LLMModelPath, Mode=TwoWay}" Margin="0,0,0,10"/>

                    <NumberBox Header="GPU Layer Count" Value="{x:Bind Path=ViewModel.GpuLayerCount, Mode=TwoWay}" />

                    <NumberBox Header="Max Token Count" Value="{x:Bind Path=ViewModel.MaxTokens, Mode=TwoWay}" />

                    <ToggleSwitch Header="Using GPU" OffContent="Disable" OnContent="Enable" IsOn="{x:Bind ViewModel.GpuEnabled, Mode=TwoWay}" Margin="0,0,0,10"/>
                </StackPanel>

                <StackPanel x:Name="GeminiSettingsPanel" Visibility="{x:Bind ViewModel.IsGeminiSelected, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
                    <TextBox Header="Gemini API Key"  Text="{x:Bind ViewModel.ApiKey, Mode=TwoWay}" Margin="0,0,0,10"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
        <Button Content="保存" HorizontalAlignment="Right" VerticalAlignment="Bottom" Click="OnSaveButtonClicked" Grid.Row="1" Margin="0,10,0,0"/>
    </Grid>
</Page>