<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AIPaste.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AIPaste.Views"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <converters:BoolToObjectConverter x:Key="BoolToVisibilityConverter" TrueValue="Visible" FalseValue="Collapsed"/>
    </Page.Resources>
    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="0">
            <StackPanel>
                <ToggleSwitch x:Uid="Settings_HotkeyEnable" IsOn="{x:Bind ViewModel.IsHotkeyEnabled, Mode=TwoWay}" Margin="0,0,0,10"/>
                
                <TextBox x:Uid="Settings_KeyPattern" Text="{x:Bind ViewModel.KeyPatternText, Mode=TwoWay}" Margin="0,0,0,10"/>

                <ToggleSwitch x:Uid="Settings_AutoStart" IsOn="{x:Bind ViewModel.AutoStart, Mode=TwoWay}" Margin="0,0,0,10"/>

                <ComboBox x:Uid="Settings_ModelType" x:Name="ModelTypesCombo"
                    ItemsSource="{x:Bind ViewModel.ModelTypes}"
                    DisplayMemberPath="Item1"
                    SelectedValuePath="Item2"
                    SelectedValue="{x:Bind ViewModel.ModelType, Mode=TwoWay}" 
                    Margin="0,0,0,10"
                    Loaded="ModelTypesLoaded"/>

                <StackPanel x:Name="LocalLLMSettingsPanel" Visibility="{x:Bind ViewModel.IsLocalLLMSelected, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
                    <TextBox x:Uid="Settings_LocalModelPath" Text="{x:Bind ViewModel.LLMModelPath, Mode=TwoWay}" Margin="0,0,0,10"/>

                    <NumberBox x:Uid="Settings_GpuLayerCount" Value="{x:Bind Path=ViewModel.GpuLayerCount, Mode=TwoWay}" />

                    <NumberBox x:Uid="Settings_MaxTokenCount" Value="{x:Bind Path=ViewModel.MaxTokens, Mode=TwoWay}" />

                    <!-- not suported UsingGpu -->
                    <!-- <ToggleSwitch x:Uid="Settings_UsingGpu" IsOn="{x:Bind ViewModel.GpuEnabled, Mode=TwoWay}" Margin="0,0,0,10"/> -->
                </StackPanel>

                <StackPanel x:Name="GeminiSettingsPanel" Visibility="{x:Bind ViewModel.IsGeminiSelected, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
                    <TextBox x:Uid="Settings_GeminiApiKey"  Text="{x:Bind ViewModel.ApiKey, Mode=TwoWay}" Margin="0,0,0,10"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
        <Button x:Uid="Settings_SavingButton" HorizontalAlignment="Right" VerticalAlignment="Bottom" Click="OnSaveButtonClicked" Grid.Row="1" Margin="0,10,0,0"/>
    </Grid>
</Page>