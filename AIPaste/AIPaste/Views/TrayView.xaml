<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="AIPaste.Views.TrayView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AIPaste.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tb="using:H.NotifyIcon"
    mc:Ignorable="d">

    <Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <tb:TaskbarIcon 
                ToolTipText="AIPaste"
            IconSource="/Images/AIPaste_trayLogo.ico"
            LeftClickCommand="{x:Bind IconClicked}"
            NoLeftClickDelay="True"
            >
                <tb:TaskbarIcon.ContextFlyout>
                    <MenuFlyout>
                        <MenuFlyoutItem x:Uid="Tray_Exit" Command="{x:Bind ExitClicked}"/>
                        <MenuFlyoutSeparator/>
                        <MenuFlyoutItem x:Uid="Tray_SettingsTitle" Command="{x:Bind SettingsClicked}"/>
                    </MenuFlyout>
                </tb:TaskbarIcon.ContextFlyout>
            </tb:TaskbarIcon>
        </StackPanel>
    </Grid>
</UserControl>
